apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "lotris.fullname" . }}-reports
  labels:
    {{- include "lotris.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.api.reportsVolumeSize }}
