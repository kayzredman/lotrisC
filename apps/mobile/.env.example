# Lotris Pager — local dev (copy to .env)
EXPO_PUBLIC_API_URL=http://192.168.100.51:5153

# Required for server push in Expo Go (iOS). Get via: npx eas-cli login && npx eas-cli init
# EXPO_PUBLIC_EAS_PROJECT_ID=your-uuid-from-expo-dev
